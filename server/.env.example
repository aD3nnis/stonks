# Copy this file to .env and fill in real values. Do not commit .env.

# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/stonks

# LiveCoinWatch API key (required) — get one at https://www.livecoinwatch.com/tools/api
LIVECOINWATCH_API_KEY=

# Optional: server port (default 3000)
# PORT=3000

# Optional: set to "true" on Render so the server uses the correct DB SSL mode
# RENDER=true
